// Datatypes:

    // let a = 10
    // let a = `hello`
    // let a = false
    // let a
    // let a = null

    // console.log(a, typeof(a));

// Operator:

    // Arithmetic operators
        // (+, -, *, /, %, **, ++, --)

            // let a = 5
            // let b = 2
            // let result

            // result = a + b
            // result = a - b
            // result = a * b
            // result = a / b
            // result = a % b
            // result = a ** b
            // result = ++a
            // result = --a

            // console.log(result);

    // Assignment operators
        // (=, +=, -=, *=, /=, %=, **=)

            // let a = 5

            // a += 2     // a = a + 2
            // a -= 2     // a = a - 2

            // console.log(a);
        
    // Comparison operators --> (true or false) -- Boolean
        // (==, ===, !=, !==, >, <, >=, <=)

            // let age = 18
            // let result

            // result = (age == 18)
            // result = (age === 18)
            // result = (age !== 18)
            // result = (age > 18)
            // result = (age < 18)
            // result = (age <= 18)

            // console.log(result);

    // Logical operators
    // Ternary operators
    // Type operator

